// ── Product Detail Page ────────────────────────────────────────────────────────
import { PageWrapper } from "../components/layout/PageWrapper";

// ── Component ──────────────────────────────────────────────────────────────────
export function ProductDetailPage() {
   return (
      <PageWrapper>
         <div className="px-4 py-8 text-center text-gray-400 text-sm">Страница товара — в разработке</div>
      </PageWrapper>
   );
}
